spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: ganti username oracle
    password: ganti password oracle
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
